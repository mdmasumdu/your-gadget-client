import { useLoaderData } from "react-router-dom";

const Details = () => {
    const phone = useLoaderData()
    console.log(phone)
    return (
        <div>
            
        </div>
    );
};

export default Details;